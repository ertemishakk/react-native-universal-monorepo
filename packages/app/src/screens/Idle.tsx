import React from "react";
import { View, Text } from "react-native";

export const IDLE_MODE = "IDLE_MODE";

const Idle = () => {
  return (
    <View>
      <Text>Idle</Text>
    </View>
  );
};

export default Idle;
